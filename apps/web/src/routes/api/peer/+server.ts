// import { PeerServer } from 'peer';

// const peerServer = PeerServer({ port: 9000, path: "/myapp" });

// app.use(peerServer);

// console.log('Listening.');

// export default {
//   path: '/peerjs',
//   handler: peerServer,
// };
